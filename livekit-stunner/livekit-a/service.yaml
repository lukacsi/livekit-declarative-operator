apiVersion: v1
kind: Service
metadata:
  name: livekit-sig
  namespace: livekit-a
spec:
  type: ClusterIP
  selector: 
    app: livekit-server
  ports:
    - name: http
      port: 7880
      targetPort: 7880
      protocol: TCP
